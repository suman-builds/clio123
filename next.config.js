/** @type {import('next').NextConfig} */
const nextConfig = {
  // Simple configuration to avoid interfering with Next.js internals
  reactStrictMode: true,
  swcMinify: true
};

module.exports = nextConfig;